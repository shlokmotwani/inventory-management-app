<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include("./header.ejs") %>

    <ul>
      <% books.map(book => { %>
      <li>
        <%= book.title %>
        <a href="<%" ="/books/" + book.id %>>Details</a>
      </li>
      <% }) %>
    </ul>

    <!-- <%- include("./footer.ejs") %> -->
  </body>
</html>
